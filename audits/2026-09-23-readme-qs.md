# README Quick Start Audit (phase 15, 2026-09-23)

Verdict: already present, line 32-58, skip.

`README.md` line 32 opens with `## Try the failure path first`, which is the
quick-start section by content (concrete Node command + expected output). It
calls `node ./cmd/crctl.js --help` and
`node ./cmd/crctl.js demo refund --fault duplicate`, then shows the expected
`compensated` outcome. Node 20+ requirement is named. Three concrete steps
plus expected result; nothing to add.
